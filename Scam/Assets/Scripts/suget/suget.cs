/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class suget : MonoBehaviour
{
    [SerializeField] private string firstSceneName = "WelcomeScene";  // Имя сцены, которая откроется при первом запуске

    void Update()
    {
        // Проверяем, первый ли это запуск
        if (Data.Instance.GetisFirstTime())
        {
            // Если первый запуск, загружаем сцену
            SceneManager.LoadScene(firstSceneName);
        }
    }

}*/
